To run application:

Run the following command:
    java -jar mario-0.0.1.jar

The application will use breadth first search to return a list of moves for Mario to make to first go to Bowser
and then Peach. If no such path is possible or input is invalid it will return an empty list.

